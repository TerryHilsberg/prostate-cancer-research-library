# Scripts

This repository is designed to be updated by a weekly Hermes cron job rather than a fixed local script. If an automated retrieval script is later added, keep it source-transparent: store queries, retrieval dates, PMIDs/DOIs, and URLs.
